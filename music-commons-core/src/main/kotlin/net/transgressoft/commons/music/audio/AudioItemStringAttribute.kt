package net.transgressoft.commons.music.audio

import net.transgressoft.commons.query.StringAttribute

enum class AudioItemStringAttribute : StringAttribute {
    TITLE, GENRE_NAME, LABEL_NAME, COMMENTS, ENCODER, ENCODING
}